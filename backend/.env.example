PORT=5000
DB_USER=postgres
DB_HOST=localhost
DB_NAME=elearning
DB_PASSWORD=changeme
DB_PORT=5432
JWT_SECRET=changeme-use-a-long-random-string

# Production only (leave unset for local dev):
# DATABASE_URL=postgres://user:pass@host:5432/db   # overrides the DB_* vars above, used with SSL
# CORS_ORIGIN=https://your-frontend.vercel.app      # overrides the localhost:5173 default
